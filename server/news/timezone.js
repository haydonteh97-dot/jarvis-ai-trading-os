export function isValidTimezone(timezone) { if (!timezone) return false; try { new Intl.DateTimeFormat("en", { timeZone: timezone }).format(); return true; } catch { return false; } }
export function formatPublishedTime(timestamp, timezone) { if (!timestamp || !Number.isFinite(Date.parse(timestamp)) || !isValidTimezone(timezone)) return { label: "Published time unavailable", timezone: isValidTimezone(timezone) ? timezone : "Timezone not confirmed" }; return { label: new Intl.DateTimeFormat("en-GB", { timeZone: timezone, dateStyle: "medium", timeStyle: "short" }).format(new Date(timestamp)), timezone }; }
