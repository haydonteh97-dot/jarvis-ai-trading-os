import { mapAffectedAssets, newsRisk, buildMarketSentiment, templateInterpretation } from "./intelligence.js";
export function scannerNewsRiskContext(articles, asset) { const relevant = articles.filter((article) => mapAffectedAssets(article).some((entry) => entry.asset === asset)); if (!relevant.length || relevant.every((article) => article.dataStatus === "demo")) return { newsRisk: "Unavailable", score: 0, dataQuality: relevant.length ? "Demo" : "Unavailable", stories: [] }; const risks = relevant.map((article) => ({ articleId: article.id, ...newsRisk(article) })); return { newsRisk: risks.some((item) => item.overallNewsRisk === "high") ? "High" : "Moderate", score: Math.min(...risks.map((item) => item.scannerScore)), dataQuality: "Verified", stories: risks }; }
export function aiAnalysisNewsContext(articles, asset) { const relevantStories = articles.filter((article) => mapAffectedAssets(article).some((entry) => entry.asset === asset)); return { relevantStories, selectedHeadline: relevantStories[0]?.headline || null, category: relevantStories[0]?.category || null, impact: relevantStories[0]?.impact || "insufficient_data", verificationStatus: relevantStories[0]?.verificationStatus || "unavailable", affectedAssets: relevantStories[0] ? mapAffectedAssets(relevantStories[0]) : [], directionalPressure: "awaiting_confirmation", marketSentiment: buildMarketSentiment(relevantStories[0]), newsRisk: newsRisk(relevantStories[0]), dataSource: relevantStories[0]?.sourceName || null, lastUpdated: relevantStories[0]?.updatedAt || relevantStories[0]?.publishedAt || null }; }
export function tradePlannerNewsRisk(articles, asset) { const context = scannerNewsRiskContext(articles, asset); return { status: context.dataQuality === "Demo" ? "Demo" : context.newsRisk === "Unavailable" ? "Source Unavailable" : context.newsRisk, highestImpactStory: context.stories[0] || null, affectedAsset: asset, sourceStatus: context.dataQuality }; }
export function askJarvisNewsHandoff(article) { return { articleId: article.id, headline: article.headline, source: article.sourceName, publishedAt: article.publishedAt, category: article.category, verificationStatus: article.verificationStatus, summary: article.summary, interpretation: templateInterpretation(article), affectedAssets: mapAffectedAssets(article), marketSentiment: buildMarketSentiment(article), riskContext: newsRisk(article), decisionStatus: article.dataStatus === "demo" ? "Preliminary" : "Monitor" }; }
