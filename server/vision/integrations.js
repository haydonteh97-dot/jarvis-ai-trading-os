export function toAskJarvisContext(upload) { return { source: "Vision Foundation", uploadId: upload.id, chartContext: upload.chartContext, imageQuality: upload.imageQuality, confidence: upload.confidence, dataStatus: upload.dataStatus, warnings: upload.warnings }; }
export function toAiAnalysisContext(upload) { return { source: "Vision Foundation", uploadId: upload.id, asset: upload.chartContext.asset, timeframe: upload.chartContext.timeframe, dataStatus: upload.dataStatus, analysisStatus: "preliminary", exactLevelsAvailable: false, warnings: upload.warnings }; }
export function toTradePlannerContext(upload) { return { source: "Vision Foundation", uploadId: upload.id, chartContextStatus: "Unavailable", asset: upload.chartContext.asset, timeframe: upload.chartContext.timeframe, entry: null, stopLoss: null, takeProfit: null, positionSizingAllowed: false, requiresUserConfirmation: true, warnings: ["Verified chart levels are unavailable. Unverified image levels cannot be used for position sizing."] }; }
